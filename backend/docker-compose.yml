services:
  php-apache-environment:
    container_name: php-url-shortener
    build:
      context: ./src
      dockerfile: Dockerfile
    volumes:
      - ./src:/var/www/html/
    ports:
      - 8005:80
